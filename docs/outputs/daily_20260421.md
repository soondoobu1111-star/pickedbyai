# 일일 세션 기록 — 2026-04-21 (화)

## 📋 세션 요약

**날짜**: 2026-04-21 (화)
**작업 환경**: 데스크탑 Claude
**핵심 이벤트**: 🔴 **BUG-CRON-SCHEDULE-KST-01** — CEO가 대시보드 Overview 차트에서 "오늘(Apr 21) 데이터 없음" 목격 → 원인: cron UTC 02:00 (= KST 11:00)이 유저 진입 시각보다 늦음 → cron 시각 UTC 15:00 (= KST 00:00) 변경 + Apr 21 데이터 즉시 생성

---

## ✅ 완료 항목

### 🔴 BUG-CRON-SCHEDULE-KST-01 (cron 시각 변경)
- ✅ **원인 분석**: cron 실행 시각이 UTC 02:00 (= KST 11:00)으로 설정 → 매일 KST 00:00~11:00 사이에 대시보드 열면 "오늘 데이터 없음" 11시간 공백 발생
- ✅ **근본 해결**: `api/wrangler.toml` cron 시각 변경 — **UTC 02:00 → UTC 15:00 (= KST 00:00 자정)**
  - 프로덕션: `crons = ["0 15 * * *"]`
  - 스테이징: `crons = ["0 15 * * *", "* * * * *"]` (retry queue 유지)
- ✅ **배포**: 스테이징 `8cf84f50` + 프로덕션 `b76cbde6`
- ✅ **Apr 21 데이터 즉시 생성**: `/v1/admin/run-cron` 엔드포인트 호출 → `dailyRefresh` 백그라운드 트리거 → 스테이징 scores 테이블에 2026-04-21 row 생성
- ✅ **임시 스테이징 무인증 우회 즉시 원복**: 호출 직후 SUPABASE_SERVICE_KEY 인증 필수로 복구 + 재배포 (`8cf84f50`)
- ✅ **CF Workers 30초 제한 대응**: admin run-cron 핸들러를 `await dailyRefresh` → `c.executionCtx.waitUntil(dailyRefresh)` 로 변경 (백그라운드 실행, 30초 제한 회피)

### 📈 기대 효과
- **내일 04-22 오전부터**: 대시보드 열면 이미 오늘 데이터 있음 (데이터 공백 11시간 → 0시간)
- **유저 첫 진입 UX**: "왜 오늘 데이터 없냐" 혼란 제거
- **비용 변화 없음**: 실행 횟수 동일 (하루 1회)

---

## 📝 결정 사항

| 항목 | 결정 | 승인 |
|------|------|------|
| cron UTC 02:00 → UTC 15:00 | ✅ 변경 | CEO 2026-04-21 09:40 KST |
| Apr 21 데이터 즉시 생성 (admin 트리거) | ✅ 실행 | CEO 2026-04-21 09:40 KST |
| 스테이징 무인증 우회 | ✅ 임시 적용 → 즉시 원복 | CPO 자율 (보안 리스크 통제) |

---

## 🔴 어제(2026-04-20) 세션 이월 — Shell 계정 드롭다운 복구 (누락 보강)

### BUG-UI-SHELL-ACCOUNT-01
- ✅ **원인**: UI-SHELL-INTEGRATE-01 Phase A 통합 시 기존 `#nav-user` 드롭다운을 Shell topbar로 이식하지 않고 이메일 텍스트만 남김 → Settings/Sign Out 접근 불가
- ✅ **수정**: Shell topbar 우측에 `#shell-avatar-btn` + `#shell-nav-dropdown` 추가, Settings/Sign Out 버튼 복원
- ✅ `toggleDropdown()` 함수가 Shell 활성화 상태에서 `#shell-nav-dropdown` 토글하도록 분기
- ✅ 로그인 시 `#shell-avatar` 이미지 + 드롭다운 email 자동 주입
- ✅ 외부 클릭 닫기 리스너에 Shell 드롭다운도 포함
- ✅ 스테이징 FE 배포 `c916485f`

### 교훈 기록
- 🔴 Shell 통합 시 **기존 UX 요소(드롭다운/메뉴/버튼)를 위치만 옮기고 기능 보존 필수** — 이전처럼 "Phase B에서 처리 예정"으로 중간 기능 누락 금지
- 🔴 **CEO 승인 없이 기존 기능 제거 금지** — UI-SHELL-INTEGRATE-01에서 `#nav-user`를 "Phase B에서 제거 예정"으로 숨긴 것이 실제로는 기능 사라짐으로 드러남

---

## 🎯 교훈 (영구 기록)

1. **운영 시각(cron, refresh 등)은 유저 현지 시각 기준으로 설계한다** — UTC 02:00이 개발자에게는 "자정"처럼 느껴져도 KST 유저에게는 오전 11시. 유저 타임존 기준 자정 직후 실행이 맞다.
2. **"오늘 데이터가 없다"는 CEO 지적은 기술적 정상이어도 UX 결함이다** — 기술 관점 "cron 아직 미실행"은 사용자 관점 "서비스가 오늘 작동 안 함"이다.
3. **임시 무인증 우회는 30초 이내 원복한다** — 배포→호출→원복→재배포를 한 호흡에 진행해서 공격 창 최소화.
4. **Shell 통합 시 기존 기능 전수 이식 → 위치만 변경, 기능 제거는 별도 CEO 승인** (BUG-UI-SHELL-ACCOUNT-01 재발 방지).

---

## 🔁 다음 액션 (04-22 수~)

1. **내일 아침 검증**: KST 00:00 cron 정상 실행 확인 → 대시보드 열면 04-22 데이터 즉시 보임
2. **D3 DOMAIN-SEL-01**: Shell 상단 도메인 드롭다운 (다중 도메인 전환)
3. **UI-SHELL-INTEGRATE-01 Phase B**: #nav-user 완전 제거 + data-pane 세분화 (Shell 드롭다운 복구된 상태에서 진행)
4. **D5 OVERVIEW-01**: Overview 12섹션 (Phase B 후)
5. **프로덕션 대시보드 Shell 배포**: 스테이징 Phase B 검증 후 CEO 승인 받아 프로덕션 반영

---

## 🔴 특별 관리 중 버그 (진행 상태)

| ID | 상태 | 비고 |
|----|------|------|
| BUG-SCORE-UNIFY-FIX-01 | ✅ 수정 완료 | 스테이징+프로덕션 배포, 백필 완료. 재발 방지 테스트 3건 Phase 1 TEST-01에 편입 필요 |
| BUG-UI-SHELL-ACCOUNT-01 | ✅ 수정 완료 (스테이징) | 프로덕션은 Shell 아직 미배포 (해당 없음) |
| BUG-CRON-SCHEDULE-KST-01 | ✅ 수정 완료 | 내일 아침 정상 실행 검증 필요 |

---

## [CPO] 세션 2 — Phase B + D5 Overview 핵심 6섹션 (데스크탑 Claude, 10:00~11:20 KST)

### CEO 승인 사항 (2026-04-21)
- **A안 확정**: Shell Phase B (뼈대 세분화) → D5 Overview 핵심 6섹션 순차 구현
- **Sources 4파일 풀셋** → 빅파이 1.5.2 신규 스프린트(05-01 이후)로 분리. 수익화 논의 모두의 창업 지원 결과 후 재개
- Tools 메뉴 아래 Sources 서브 + 온보딩 4스텝 확장 권장

### 완료 작업
1. **UI-SHELL-INTEGRATE-01 Phase B** — dashboard.html 3150→3349줄
   - 기존 `#tab-mine`·`#tab-tools`와 공존하던 단일 placeholder 구조를 6개 독립 pane으로 재구성
   - 4개 신규 pane (`#shell-pane-trend`·`#shell-pane-rivals`·`#shell-pane-journey`·`#shell-pane-actions`) 추가, 각 pane은 "Coming D{n}" 티저 (Trend/Journey=D6 · Rivals=D8 · Actions=D9a)
   - `shellNavigateTo()` 함수 전면 교체 — `pane-{key}` body class 마커 + `OVERVIEW_ONLY_IDS` 제어(shell-d5-overview / check-form-card / check-result / guide-email-cta) + history-card/tab-mine/tab-tools 분기 + 신규 pane 4개 토글
   - 기존 `.dash-tabs-bar`(My Products/Tools 2탭) Shell 활성화 시 자동 숨김 (사이드바가 대체)
   - `#check-form-card`·`#history-card` ID 부여 (pane 제어용)

2. **UI-OVERVIEW-01 D5 핵심 6섹션**
   - 상단 D5 Header: Representative Domain + Verified 배지 + Last Check 타임스탬프
   - Empty State: verified 도메인 없을 때 "Register Your Domain" CTA
   - Section 1 · 큰 점수 링 (SVG r=78, stroke-dasharray 애니메이션) + Pass Indicator (🏆 PERFECT / 🟢 STRONG / 🟡 EMERGING / 🔴 INVISIBLE)
   - Section 2 · Daily Pulse 카드 (unified.insights.daily_pulse 표시, Δ last 24h — D6 연동 예정)
   - Section 3 · 4차원 Breakdown grid (auto-fit 220px, sum 실시간 표시)
   - Section 4 · Engine Grades (Gemini/Perplexity 2엔진 A~F 배지)
   - Section 5 · 2D Quadrant (1:1 박스, Recognition X축·Category Y축, 점 위치 5~95% 맵)
   - Section 6 · 카테고리 벤치마크 바 (avg line + you marker, D7 Probe 재설계 후 실측값 fetch)

3. **데이터 바인딩**
   - `pickRepresentativeProduct()` 신설 — verified 우선 → 최고점 fallback
   - `renderD5Overview()` — unified_v15 JSONB 전체 매핑 (score/dimensions/pass_indicator/quadrant/engine_grades/insights)
   - `loadHistory()` 완료 시 자동 renderD5Overview 호출 (pane-overview일 때만)

### Playwright MCP QA 결과
| 시나리오 | 결과 |
|---------|------|
| Overview 진입 | ✅ d5-main-grid `block` · check-form `block` · tab-mine `block` · 4 pane `none` |
| Trend 진입 | ✅ shell-pane-trend `block` · 나머지 모두 `none` · crumb "Trend" |
| Rivals 진입 | ✅ shell-pane-rivals `block` · 나머지 `none` · crumb "Rivals" |
| Journey 진입 | ✅ shell-pane-journey `block` · crumb "Journey" |
| Actions 진입 | ✅ shell-pane-actions `block` · crumb "Actions" |
| Tools 진입 | ✅ tab-tools `block` · history-card `block` · crumb "Tools" |
| 실제 데이터(pickedby.ai) | ✅ score 27.5 · sum 17.5+0+0+10=27.5 **수학적 일관성** · pass "strong" · Gemini F · Perplexity A |
| 콘솔 에러 | ✅ 0건 |

### 배포 버전
- FE 스테이징: `2bd516c3-b2de-4290-9dcf-0983724231f7` (staging-0404.pickedby.ai)
- API 스테이징: `8562d6d9` (cron 2개 정상 등록)
- 프로덕션 배포 없음 (D11 DEPLOY-GATE-01 통과 후 CEO 승인 필요)

### 신규 파일
- 없음 — 기존 `landing/dashboard.html` 단일 편집으로 전 범위 흡수

### 백로그 신설 항목
- 빅파이 1.5.2 Sources 스프린트 5건 (SITEMAP/RSS/ROBOTS/LLMS/UI) — 05-01 이후
- UI-OVERVIEW-12FULL — Overview 잔여 6섹션 (미니차트·마일스톤 등)

### 교훈 (이번 세션)
1. **"단일 화면 6개 메뉴"가 "6개 콘텐츠"가 아니면 Shell 의미 없음** — Phase A에서 이를 방치한 게 CEO 질문으로 드러남. Phase B 필수성 증명.
2. **unified_v15 JSONB 한 번 잘 설계하면 재활용이 무한** — renderUnifiedExtras / buildRowHtml / renderD5Overview 모두 동일 소스 사용. 이원 시스템 폐기가 여기서 복리로 작용.
3. **Sources는 진짜 모트** — 경쟁사(Otterly/Profound) 0개 vs 우리 4개. 카테고리 자체를 정의할 기회. 다만 모두의 창업 지원 후 수익화 전략과 연동.
4. **Playwright MCP가 Phase B 검증의 단일 진실 소스** — 콘솔 상태 + 네트워크 + DOM 동시 검증. sum==final 수학적 일관성도 JS로 즉시 확인 가능.

### 다음 액션 (04-22 수 이후)
1. **내일 아침 검증** (04-22): KST 00:00 cron 자동 실행 → 04-22 scores row 생성 확인 (BUG-CRON-SCHEDULE-KST-01 재발 방지 검증)
2. **D3 DOMAIN-SEL-01** Shell 상단 도메인 드롭다운 (다중 도메인 전환)
3. **D6 TREND-01 + JOURNEY-01** (04-23 목) — Trend 3탭 차트 + Journey Hero's Arc
4. **D7 PROBE-REDESIGN-01** (04-24 금) — D안 역방향 스무고개 백엔드
5. **UI-OVERVIEW-12FULL** Overview 12섹션 전체 완성 (카테고리 실측 fetch·7d 미니차트·마일스톤)

---

*2026-04-21 세션 — CPO 긴급 대응 2건 + 누락 보강 1건 + Phase B·D5 Overview 6섹션 + Sources 스프린트 신설*

---

## [CPO] 세션 3 — B안 전면 개편 (Overview 하단 정리) 데스크탑 Claude, 11:30~12:15 KST

### CEO 피드백 3건 (2026-04-21 오전)
1. 헤더 우측 상단 이메일 텍스트가 avatar 드롭다운과 **중복**
2. CHANGELOG 배너가 **모든 메뉴에 반복 노출** — Overview 전용이어야 함
3. Overview 하단에 기존 Check form / Check result / Guide CTA / My Products 목록이 **그대로 남아있음** — 처리 방침?

### CPO 3안 제시 → CEO B안 선택 (전면 재배치)
- A안 유지 · B안 전면 재배치 · C안 점진적 중 **B안 확정**

### 구현 요약 (dashboard.html 3349→3555줄)

| 변경 | Before | After |
|------|--------|-------|
| 헤더 email | `#shell-topbar-email` span 표시 | 삭제 (드롭다운 내부 `#shell-nav-dropdown-email`만 유지) |
| CHANGELOG beta-banner | 모든 pane 노출 | Overview 전용 (`OVERVIEW_ONLY_IDS`에 편입) |
| Check AI Visibility 카드 | Overview 하단 고정 | **Shell topbar "+ Check" 노란 버튼 + Quick Check 모달 오버레이** 로 대체. DOM 완전 제거, input IDs (input-product/input-url/btn-check) 모달 안으로 이동 |
| Check result 카드 | 체크 직후 Overview 하단 전개 | **토스트 + Overview 점수링 자동 갱신**. `#check-result` DOM 유지(showResult 데이터 세팅)하되 `SHELL_ALWAYS_HIDDEN_IDS`로 항상 숨김 |
| Guide Email CTA | Overview 하단 (`#guide-email-cta`) | **Actions pane 내부 이식** (`#actions-guide-email` / `submitActionsGuideEmail` 전용 핸들러) · 원본은 숨김 |
| My Products 목록 | History card 내부 `#tab-mine` | **Overview "Recently Added" 섹션** (top 5 최신, 배지+점수+날짜) · `history-card` Tools 전용 |

### 신규 컴포넌트
- **Quick Check Modal** (`#quick-check-overlay`) — ESC 닫기 + 외부 클릭 닫기 + 체크 완료 후 자동 닫힘 + 제품명 autofocus
- **Toast** (`#pba-toast`) — 우측 하단 고정, 3.4초 auto-hide, 골드 픽셀 오프셋 그림자
- **`openQuickCheck()` / `closeQuickCheck()` / `showToast()`** 전역 함수
- **`submitActionsGuideEmail()` / `toggleActionsGuideBtn()`** — 기존 submitGuideEmail 분리 복제
- **체크 완료 훅** (runCheckForce 말미) — `closeQuickCheck()` + `showToast(✓ Checked: name — score/100 tier)` + `shellNavigateTo('overview')` 자동 이동 + renderD5Overview 자동 재호출

### 데이터 아키텍처 결정
- **체크 결과 카드 폐기 vs DOM 유지**: DOM 유지 선택. showResult()가 여전히 result-product/result-score/result-dimensions/result-v15-extras 채우지만 시각적으로 안 보임. 장점: 기존 feedback-cta·result-email-cta·mlp-track-cta 분기 로직 유지 비용 0.
- **My Products 대체**: "Recently Added" 요약은 상위 5개만 표시. 전체 도메인 관리는 D3 DOMAIN-SEL-01에서 Shell topbar 도메인 드롭다운으로 구현 예정.

### Playwright MCP QA 결과 (스테이징 Version `dd8f86d7`)
| 시나리오 | 결과 |
|---------|------|
| Quick Check 버튼 존재 | ✅ `shell-quick-check-btn` FOUND |
| Modal open/close | ✅ display `none → flex → none` |
| Toast 표시 | ✅ `pba-toast` display `block` |
| 6 pane 스위칭 | ✅ 전수 통과 (overview/trend/rivals/journey/actions/tools) |
| check-form-card DOM 제거 확인 | ✅ DOM count 0 (주석만 남음) |
| beta-banner Overview 전용 | ✅ trend/rivals/journey/actions/tools 모두 `none` |
| Actions pane 내 Guide CTA | ✅ `#actions-guide-email` FOUND |
| Recently Added 렌더 | ✅ pickedby.ai / notion / capcut 3개 제품 표시 (33 · 33 · 33 · Apr 21) |
| 콘솔 에러 | ✅ 0건 |

### 배포 버전
- FE 스테이징: `dd8f86d7-bdd6-41c5-ae9a-b784e73c7468`
- 프로덕션 배포 없음 (D11 DEPLOY-GATE-01 통과 후 CEO 승인 필요)

### D3 스프린트 선결 효과
- Shell topbar "+ Check" 버튼 이미 배치됨 → D3 DOMAIN-SEL-01은 도메인 드롭다운만 추가하면 됨
- My Products 전체 관리 UI → D3 도메인 드롭다운 안으로 이전 설계 유지

### 교훈 (이번 세션)
1. **"6메뉴가 똑같은 화면이면 Shell은 의미 없음"** — Phase B에서 pane 분리만으로는 부족. 각 pane의 정체성(Overview="지금 내 상태")을 지키려면 **무관한 블록을 과감히 이동/제거**해야 함.
2. **ID 중복이 기능 충돌 원인** — check-form-card의 input-product/input-url/btn-check가 Quick Check 모달과 충돌 → 기존 DOM 완전 제거로 해결. getElementById는 첫 번째 매칭만 반환하는 특성 주의.
3. **"전면 개편"은 한 번에 하는 게 리스크가 적다** — C안(점진적)은 D3에서 또 건드려야 함. B안(전면)은 Overview 정체성을 확실히 심고 D3는 드롭다운에만 집중. CEO 판단 옳았음.
4. **data-pane 분리만큼이나 "무엇이 속하지 않는가"가 중요** — OVERVIEW_ONLY_IDS / SHELL_ALWAYS_HIDDEN_IDS 두 리스트로 명시적 관리. 향후 D5 확장 시 실수 방지.

### 다음 액션 (04-22 수)
1. 내일 아침 KST 00:00 cron 자동 실행 검증 (BUG-CRON-SCHEDULE-KST-01)
2. **D3 DOMAIN-SEL-01** Shell topbar 도메인 드롭다운 + 다중 도메인 전환 (My Products 전체 관리 UI 포함)
3. **D6 TREND-01 + JOURNEY-01** — 3탭 차트 + Hero's Arc

---

*2026-04-21 세션 최종 — CPO 세션 3건 누적 (cron 버그 / Shell 드롭다운 / B안 전면 개편) + D5 Overview 6섹션 + Sources 스프린트 신설 + Overview 정체성 확립*

---

## [CPO] 세션 4 — D3 DOMAIN-SEL-01 + Recently Added 점수 버그 수정 (데스크탑 Claude, 12:30~13:10 KST)

### CEO 피드백 2건 (B2 수정 요청)
1. 헤더 "+ Check" 위치가 Recently Added "+ Register"와 자리 바꾸기 + **헤더에는 드롭다운으로 현재 선택된 제품명 + 제품등록 통합**
2. Recently Added에 pickedby.ai / notion / capcut **모두 33점** — 말이 안 됨

### 원인 조사 (Playwright 실데이터)
```
pickedby.ai: row.score=33, unified_v15.score=27.5
notion:      row.score=33, unified_v15.score=45
capcut:      row.score=33, unified_v15.score=22.5
```
→ **`row.score` 세 제품 모두 33으로 하드코딩** (API `/v1/check`의 레거시 Tavily fallback 경로 의심). FE가 `row.score`를 사용해서 세 제품 모두 33 렌더. 빅파이 1.5 "이원 시스템 폐기" 원칙 위배.

### 구현 (dashboard.html 3555→3640줄)

**1) 헤더 도메인 드롭다운 (D3 DOMAIN-SEL-01 당겨 실행)**
- `#shell-domain-picker` 신설 (Shell topbar 좌측 breadcrumb 옆)
- `#shell-domain-btn`: 현재 대표 제품명 + ▼ (예: "pickedby.ai ▼")
- `#shell-domain-menu`: 제품 목록 + 구분선 + "+ Register new domain"
- 각 제품 행: ✓ (현재 선택) + 제품명 + verified 배지 + unified_v15.score
- verified 우선 → 최고점 순 정렬
- 외부 클릭 시 자동 닫힘

**2) 선택 상태 관리**
- `_selectedDomainName` 전역 플래그 (null이면 자동 대표)
- `selectDomain(name)` → 라벨 갱신 + 메뉴 닫기 + `renderD5Overview()` 즉시 재호출
- `pickRepresentativeProduct()` 우선순위: **선택된 제품 > verified 최고점 > 전체 최고점**
- 점수 비교도 `unified_v15.score` 우선 사용

**3) Recently Added 점수 버그 수정**
- 기존: `const s = Math.round(Number(p.score) || 0)` → 모두 33
- 수정: `const s = Math.round(Number(p.unified_v15?.score ?? p.score) || 0)` → 정확한 4차원 점수
- 배지 체계: Gold/Silver/Bronze(5차원 threshold) → **PERFECT/STRONG/EMERGING/INVISIBLE** (빅파이 1.5 pass_indicator 기반)
- 색상: `passColors` 적용 (perfect #FFD700 / strong #4ade80 / emerging #f59e0b / invisible #e05252)

**4) + Check 버튼 위치 이동**
- 헤더 `#shell-quick-check-btn` 완전 제거
- Recently Added 섹션 헤더 우측 "+ Register" 링크 삭제 → **"+ Check" 노란 버튼** 배치
- Register 진입점은 드롭다운 내부 "+ Register new domain" 링크로 일원화

**5) 자동 갱신 훅**
- `loadHistory()` 말미에 `refreshDomainLabel()` 추가 → 로그인 직후/체크 직후 드롭다운 라벨 자동 세팅

### Playwright MCP QA 결과 (스테이징 `27c7abf0`)
| 시나리오 | 결과 |
|---------|------|
| `shell-domain-btn` 존재 | ✅ |
| `refreshDomainLabel()` 초기 라벨 = "pickedby.ai" | ✅ |
| 드롭다운 open → 3개 아이템 렌더 | ✅ (verified+score 표시) |
| `selectDomain('notion')` → 라벨 변경 | ✅ afterSelectLabel="notion" |
| `_selectedDomainName` 전역 저장 | ✅ "notion" |
| D5 Overview 큰 점수 링 = 45 (notion) | ✅ |
| D5 Representative Domain 라벨 = notion | ✅ |
| **Recently Added 점수 버그 수정 검증** | ✅ pickedby.ai **28** · notion **45** · capcut **23** (세 제품 모두 다름, 33 하드코딩 사라짐) |
| 헤더 + Check 버튼 제거 | ✅ `shell-quick-check-btn` false |
| Recently Added "+ Check" 버튼 | ✅ FOUND |
| 드롭다운 재선택 pickedby.ai → 링 27.5 복귀 | ✅ |
| 콘솔 에러 | ✅ 0건 |

### 배포 버전
- FE 스테이징: `27c7abf0-3fe2-4452-b338-866a40fdcc57`
- 프로덕션 배포 없음

### 잔여 이슈 (이번 세션 범위 밖)
- **API `pass_indicator` 계산 로직 검토 필요** — capcut unified_v15.score=22.5인데 `pass_indicator='strong'` 반환. 22.5 < 36이므로 'invisible'이어야 정합. `api/src/unifiedScore.ts`의 pass_indicator threshold 로직 재검토. 별도 버그 리포트 BE 수정 필요.
- **`/v1/check` API의 `row.score` 필드 하드코딩 33 버그** — 빅파이 1.5 unified 시스템 전환 후 레거시 필드. Phase 1 TEST-01 자동 테스트에 편입 권장.

### 교훈 (이번 세션)
1. **CEO 직관 > 데이터 추론** — "3개 점수가 같다"는 CEO 지적이 즉각적으로 데이터 레이어 버그 포착. 빅파이 1.5 §15 교훈 #6 "CEO 직관이 수학보다 빠른 순간" 재확인.
2. **이원 시스템 폐기는 완료된 것처럼 보이지만 FE 렌더 곳곳에 잔재가 남음** — 상위 점수 링은 unified_v15 쓰면서 하단 목록은 row.score 쓰는 불일치. "하나의 진실 소스" 원칙을 모든 렌더 함수에서 재검토 필요.
3. **헤더는 "제품 관리 허브" 역할이 자연스러움** — 도메인 드롭다운(선택+등록+상태) 통합하니 Check form이 헤더에 있을 이유가 사라짐. Recently Added가 체크 진입점이 되는 게 더 논리적.
4. **드롭다운은 D3 공식 착수 전이었지만 B2 피드백으로 당겨 실행** — 미루는 것보다 CEO가 필요로 하는 순간에 통합하는 게 리스크 작음.

### 다음 액션 (04-22 수~)
1. **BUG-PASS-INDICATOR-01** (신규) — `api/src/unifiedScore.ts` pass_indicator threshold 로직 재검토 (capcut 22.5→strong 버그)
2. **BUG-SCORE-FIELD-LEGACY-01** (신규) — `/v1/check` API row.score 33 하드코딩 원인 조사 + 제거
3. **D6 TREND-01 + JOURNEY-01** (04-23 목) — Trend 3탭 차트 + Hero's Arc
4. **UI-OVERVIEW-12FULL** — Overview 잔여 6섹션

---

*2026-04-21 세션 최종 — CPO 세션 4건 누적 (cron 버그 / Shell 드롭다운 / B안 전면 개편 / D3 드롭다운+점수 버그) + 드롭다운 당겨 실행 + 레거시 데이터 버그 포착*

---

## [CPO] 세션 5 — OWNER-ONLY-01 복원 (CEO 재지적) 데스크탑 Claude, 13:15~13:45 KST

### CEO 지적 (강한 톤)
> "노션하고 capcut은 단순 검색인데 저기 왜 들어가!! 저긴 내 소유 인증이 완료된 제품목록만 들어가는거지!!"

### 원인 분석
- **2026-04-17 저녁 원복 기록** (daily_20260417.md 말미): "`runCheckForce` if(currentUser) 블록 = 프로덕션 스타일 단순화 (무조건 optimistic update)" — 이 원복이 **OWNER-ONLY-01 원칙을 실질적으로 무효화**
- 이후 체크한 모든 제품(notion/capcut 포함)이 verified 여부 상관없이 `allProducts.push` + `saveHistory('mine')` 실행 → 스테이징 DB scores 테이블 오염
- D5 renderD5Overview Recently Added / renderDomainMenu / pickRepresentativeProduct 모두 verified 필터 없음 → DB 오염이 그대로 UI 노출
- 빅파이 1.5 §2 "단일 시스템" + OWNER-ONLY-01 (2026-04-17 CEO 확정) 동시 위배

### 수정 (dashboard.html 4곳)

**1) `runCheckForce` (line ~1691) — verified 조건 복원**
```js
const isVerified = (verifiedDomains || []).some(d => ...)
  || localStorage.getItem(`pba_verified_${product}`) === '1'
if (isVerified) {
  // allProducts.push + renderMineTab + saveHistory
}
```
→ non-verified 체크는 결과 표시만 되고 DB/allProducts/My Products 어디에도 기록되지 않음.

**2) `pickRepresentativeProduct()` — non-verified fallback 제거**
- verified 제품 없으면 null 반환 → renderD5Overview가 자동으로 empty state 표시
- 선택된 도메인(`_selectedDomainName`)도 verified 검증 후에만 반영

**3) Recently Added 필터**
```js
const _verifiedNames = new Set((verifiedDomains || []).map(d => ...))
const recent = allProducts.filter(p => !p._domainOnly && _verifiedNames.has(...))
```

**4) `renderDomainMenu()` 필터**
- verified 제품만 드롭다운에 표시
- Empty state: "No verified domains yet. Register + verify to add."

### Playwright MCP QA 결과 (스테이징 `ef1c17aa`)
| 검증 | 결과 |
|------|------|
| allProducts 개수 | 3 (DB 잔존: pickedby.ai, notion, capcut) |
| verifiedDomains 개수 | 1 (pickedby.ai만) |
| Domain Picker 아이템 | **1개만 표시** (pickedby.ai) ✅ |
| Recently Added 줄 수 | **1개만** (STRONG · pickedby.ai · 28 · Apr 21) ✅ |
| notion/capcut 노출 | ✅ 제외됨 |
| D5 Overview 대표 | pickedby.ai 27.5 정상 |
| 콘솔 에러 | 0건 |

### 🔴 잔여 조치 (CEO 승인 필요)
**스테이징 DB scores 테이블 오염 정리**:
```sql
DELETE FROM scores
WHERE user_id = '{CEO user_id}'
  AND product_name IN ('notion', 'capcut');
```
→ FE 필터로 즉시 영향은 없지만, 다중 유저 환경 / 향후 분석 시 오염 제거 필수.
→ 실행 전 CEO 승인 대기.

### 배포 버전
- FE 스테이징: `ef1c17aa-3d0c-4dcc-88c4-7d191b502075`
- 프로덕션 배포 없음

### 교훈 (영구 기록)
1. **롤백도 "원칙의 롤백"을 동반할 수 있다** — 2026-04-17 저녁 원복 기록에 "프로덕션 스타일 단순화"로만 표기됐을 뿐, **OWNER-ONLY-01 원칙을 무효화한 것**은 명시되지 않았음. 롤백 영향 분석 시 "어떤 정책이 함께 무효화되는가"를 반드시 기록 필요.
2. **UI 필터와 DB 필터는 다른 층** — FE에서 verified 필터를 해도 DB에 이미 오염된 row는 남음. 두 층 모두 정비해야 완전 해결.
3. **"단순 검색 vs 소유 검증"의 경계는 제품 철학이다** — CEO 강한 톤으로 지적. Recently Added/Domain Picker = 내 자산, Check 결과 = 외부 검색. 두 가지를 섞으면 사용자는 "내 것"과 "조회한 것"을 구별 못 함.
4. **CEO 강조 표현("!!")은 원칙 위반의 증거** — 단순 버그가 아닌 철학 위반일 가능성. 즉시 원칙 확인 필요.

### 다음 액션
1. 🔴 **스테이징 DB 정리 CEO 승인** — notion/capcut row DELETE
2. **BUG-PASS-INDICATOR-01 / BUG-SCORE-FIELD-LEGACY-01** 재확인 (이전 세션 잔여)
3. **프로덕션 DB 점검** — pickedby.ai 프로덕션에도 오염 row 있는지 검토

---

*2026-04-21 세션 최종 — CPO 세션 5건 누적 (cron / Shell / B안 / D3+점수 / OWNER-ONLY-01 복원) + 롤백 원칙 추적 교훈 확립*

---

## [CPO] 세션 4 — 모두의 창업 2026 지원서 초안 작성 (데스크탑 Claude, 저녁 KST)

### 작업 내용

**Patrick Winston "How to Speak" (MIT OCW)** 원칙을 pickedby.ai 사업 자료에 적용해 모두의 창업 지원서 초안 완성.

**분석한 문서 목록:**
- Patrick Winston "How to Speak" (22페이지 전문 분석)
- pickedbyAI/docs/bizplan.md (사업계획서 최신)
- pickedbyAI/docs/bigpie-v1.5.md (4차원 스코어 전략)
- pickedbyAI/docs/whitepaper.md (Moat 전략)
- agents/products/pickedbyai.md (제품 컨텍스트)
- docs/input/지원자_장천규_이력서.pdf (MAU 600만 loplat 기획자, 블록체인 거래소 경험)
- memory/project_modoo_startup_2026.md (지원 전략)

**Patrick Winston → 지원서 매핑:**

| Winston 원칙 | 적용 |
|------------|------|
| Empowerment Promise | 한 줄 정의 + 첫 문단 "왜 지금인가" |
| Cycling (3회 반복) | "크리에이터 전용 GEO 도구 0개" → 문제/솔루션/차별점 각 1회 |
| Build a Fence | 경쟁사 비교표 (Otterly $500+ / Profound Fortune500 vs 우리 Free) |
| Winston's Star | Symbol(크라운배지) + Slogan(AI 시대 서치콘솔) + Surprise(24개 중 0개) + Salient(Flywheel) + Story(10년 기획자) |
| Contributions | 역량 섹션: "이미 달성한 것들" — 베타 100명 SOLD OUT·Reddit 5.3K뷰·13블로그 |
| No "Thank you" | 마지막은 심사위원이 기억할 한 문장으로 마무리 |

**산출물:** `pickedbyAI/docs/outputs/modoo_application_20260421.md`

### CEO 확인 요청 사항

1. **THUNOVA 흔적 정리** — pickedby.ai 도메인·푸터·약관에서 THUNOVA 표기 제거 필요 (지원서 제출 전)
2. **장천규 창업 스토리 검증** — "Gumroad에 직접 제품 올렸다가 AI에 안 보인 경험" 실제 스토리 기반으로 수정
3. **최신 수치 업데이트** — 제출 직전 베타 인원·Reddit 뷰 등 실시간 수치 반영
4. **육아휴직 창업 스토리** — 긍정적 서사 요소 (절박함·자생력·빌딩 인 퍼블릭) 활용 여부 CEO 판단

---

*[CPO] 모두의 창업 지원서 초안 완료 — Patrick Winston 원칙 7개 적용 (데스크탑 Claude)*

---

## [CPO] 세션 6 — P0 3건 완료 (BUG-PASS-INDICATOR-01 · BUG-SCORE-FIELD-LEGACY-01 · TEST-CRON-UNIFIED-01~03) 데스크탑 Claude, 14:55~15:10 KST

### CEO 지시 (2026-04-22 P0 진행)
1. cron 검증 — KST 00:00 자동 실행 확인
2. BUG-PASS-INDICATOR-01 — capcut 22.5점→strong 버그
3. BUG-SCORE-FIELD-LEGACY-01 — /v1/check row.score 33 하드코딩
4. TEST-CRON-UNIFIED-01~03 — Phase 1 TEST-01 cron mock + unified_v15 검증

### 작업 1 — cron 검증 ⏸️ 내일 아침 보고
- 현재 KST 14:54, 다음 cron UTC 15:00 (= KST 00:00) 실행까지 9시간 대기
- 04-21 row 1건(pickedby.ai score=33) 정상 존재 확인. 04-22 row 생성은 내일 아침 별도 보고

### 작업 2 — BUG-PASS-INDICATOR-01 (B안 · 근본 해결)
**CEO 승인 B안**: signal 단독 평가 폐기 → **signal AND score 양쪽 AND 조건**
- A안(score bucket만) 거부 사유: Pass Indicator 존재 이유 상실 (score 중복 표현)
- B안 채택 사유: 빅파이 1.5 §4-3 "보조 시각화" 원칙 유지 + UX 불일치 제거 + 조작 방지

**수정 `api/src/unifiedScore.ts:236~252` computePassIndicator**:
```
perfect    : signal total ≥ 4 AND score ≥ 60
strong     : signal total ≥ 2 AND score ≥ 35
emerging   : signal total ≥ 1 AND score ≥ 15
invisible  : else
```
- 시그니처 확장: `(gemini?, perplexity?, finalScore?)` — 기존 호출부(assembleUnifiedScore line 321)에 `finalScore` 추가 전달
- 검증 결과: capcut signal=2 + score=22.5 → `emerging` (기존 `strong` 버그 수정) ✅

### 작업 3 — BUG-SCORE-FIELD-LEGACY-01 (5곳 수정)
**원인:** `scores.score` 컬럼과 `scores.unified_v15.score`가 이원 계산 (probe_score 33 vs 실제 4차원 22.5). 빅파이 1.5 "단일 시스템" 원칙 위배.

**수정 5곳:**
1. `landing/dashboard.html:2200~2212` saveHistory update — `score: __persistScore` (unified 우선)
2. `landing/dashboard.html:2219~2228` saveHistory insert — 동일
3. `landing/dashboard.html:2238~2253` autoSave update — 동일
4. `landing/dashboard.html:2256~2265` autoSave insert — 동일
5. `api/src/index.ts:978~1000` cron dailyRefresh — `savedScore = Math.round(cronUnifiedV15.score)` (unified 성공 시 덮어쓰기)

**검증:** 스테이징 FE fetch로 직접 확인 — `score: __persistScore` 4건 ✅ · 레거시 패턴 0건 ✅

### 작업 4 — TEST-CRON-UNIFIED-01~03 (신규 파일 + 14 테스트)
**신규 파일:** `api/src/score.test.ts` (234줄)
- `package.json`: vitest@^1.6.0 devDependency + `test` / `test:watch` 스크립트
- `tsconfig.json`: `exclude: ["src/**/*.test.ts", "node_modules"]` 추가 (프로덕션 번들 영향 차단)

**테스트 14건 전수 PASS:**
- Invariants 2건 (차원 만점 합 = 100, INVARIANTS 4차원/v1.5.0 고정)
- TEST-01 Mathematical Consistency 3건 (sum==final 제로/부분/최대 입력)
- TEST-CRON-UNIFIED-01 Pass Indicator B안 6건 (capcut emerging / 완전인식 strong / 만점 perfect / signal=0 invisible / threshold boundary)
- TEST-CRON-UNIFIED-02 cron savedScore 통일 1건 (unified.score === savedScore)
- TEST-CRON-UNIFIED-03 unified_v15 JSONB 완전성 2건 (필드 전수 / signal 0 → invisible)

**실행 결과:** `npm test` → 14 passed / 0 failed / 1.94s

### 배포
- **API 스테이징:** `1f2f0413-ced4-49e7-b41f-3db2f6f7499c` (cron 2개 정상 등록)
- **FE 스테이징:** `d84864c4-4536-48f8-a63c-193c531e0460`
- **Gemini Relay 스테이징:** 동시 배포
- 프로덕션 배포 없음 (D11 DEPLOY-GATE-01 통과 후 CEO 승인 필요)

### Playwright MCP QA 결과
| 검증 | 결과 |
|------|------|
| FE `__persistScore` 문자열 존재 | ✅ 4건 매칭 |
| 레거시 `score: Math.round(lastCheckResult.score)` 잔존 | ✅ 0건 |
| BUG-SCORE-FIELD-LEGACY-01 주석 | ✅ 확인 |
| 6 pane 전수 존재 (overview/trend/rivals/journey/actions/tools) | ✅ |
| Shell 요소 (Quick Check / Domain Picker / Toast / D5 Overview) | ✅ 전부 FOUND |
| check-form-card DOM 제거 유지 (UI-B-REBUILD-01) | ✅ |
| console 에러 | ✅ 0건 (warning 1건만) |

### 교훈 (이번 세션)
1. **근본 해결 기준 = "컴포넌트 존재 이유 유지"** — A안(score bucket)은 Pass Indicator를 score의 재표현으로 축소. B안은 독립 정보 가치 유지. "redundant한 fix"는 근본 해결이 아님.
2. **signal AND score 양쪽 증거 일관성 = 노이즈 방지** — 1회 운 좋은 Perplexity recommended로 'strong' 승격되는 데이터 신뢰성 문제 차단.
3. **테스트 인프라는 한 번만 깔면 복리 자산** — vitest 설치 + score.test.ts 14건이 이후 Phase 1 TEST-01 전체·Phase 2 회귀·PROBE-REDESIGN-01 GT 검증까지 확장 가능. tsconfig exclude로 프로덕션 번들 오염 0.
4. **CEO가 "근본 원인 해결 선택"을 명시한 순간 = 컴포넌트 설계 의도 재확인 타이밍** — 단순 fix vs 근본 fix의 기준은 "빅파이 1.5 설계 원칙에 부합하는가"였음.

### 🔴 잔여 조치 (CEO 승인 필요)
- **04-21 pickedby.ai score=33 레거시 row 백필** — 03-21 00:44 cron 기록. FE 수정으로 새 체크는 unified 저장되지만 기존 row는 여전히 33. 수동 백필 or 유저 재체크까지 대기 중. 스테이징만이라 급하지 않음.
- **프로덕션 배포** — DEPLOY-GATE-01 통과 후 CEO 승인 받아 일괄 반영 예정.

### 다음 액션 (04-22 수 아침)
1. **KST 00:00 cron 검증** — 04-22 scores row 생성 확인 + unified_v15 not null + score===unified.score 검증
2. **D3 잔여 작업** — Domain Picker 다중 도메인 전환 edge case
3. **D6 TREND-01 + JOURNEY-01** (04-23 목) — Trend 3탭 차트 + Hero's Arc

---

*2026-04-21 세션 최종 — CPO 세션 6건 누적 (cron / Shell / B안 / D3+점수 / OWNER-ONLY-01 / P0 3건 BUG+TEST) + B안 근본 해결 + vitest 인프라 확립*

## [CPO] 작업 완료 - BUG-PASS-INDICATOR-01 B안 · BUG-SCORE-FIELD-LEGACY-01 5곳 · TEST-CRON-UNIFIED-01~03 14건 PASS (데스크탑 Claude, 15:10 KST)
