# 일일 세션 기록 — 2026-04-20 (월)

## 📋 세션 요약

**날짜**: 2026-04-20 (월)
**작업 환경**: 데스크탑 Claude (Opus 4.7 1M)
**핵심 이벤트**: 🔴 **BUG-SCORE-UNIFY-FIX-01 긴급 수정** — CEO가 스테이징 대시보드에서 구형 5차원 표시 목격 → CPO 원인 분석 → cron unified_v15 누락 확인 → 스테이징+프로덕션 배포 완료

---

## ✅ 완료 항목

### 🔴 긴급 버그 수정 (SCORE-UNIFY-FIX-01)
- ✅ **원인 분석**: cron `dailyRefresh` 가 scores 저장 시 `unified_v15` 누락 → 매일 02:00 UTC 에 null 로 덮어쓰기 → FE 수동 체크 4차원이 다음날 구형 5차원으로 "롤백"
- ✅ **코드 수정**: `api/src/index.ts` dailyRefresh 내부에 `UNIFIED_SCORE_V15` 플래그 분기 추가, `buildDimensionContext` + `computeUnified` 호출 후 `unified_v15` INSERT 에 포함
- ✅ **tsc 통과**: 에러 0건
- ✅ **스테이징 배포**: `pickedbyai-api-staging` Version `edc51247-0e29-40f9-8509-0152493631e2`
- ✅ **프로덕션 배포**: `pickedbyai-api` Version `874ee24c-3990-4359-b8f3-688c4220dced` (플래그 off 유지, 코드만 반영)
- ✅ **버그 리포트 작성**: `pickedbyAI/docs/bugs/BUG-SCORE-UNIFY-FIX-01.md` (8섹션, 재발 방지 테스트 3건 명시, 프로덕션 플래그 활성화 체크리스트 포함)
- ✅ **특별 관리 지정**: CEO 직접 지시로 향후 Phase 1 TEST-01 에 재발 방지 테스트 3건 필수 포함

---

## 🔴 특별 관리 항목 (BUG-SCORE-UNIFY-FIX-01)

### 즉시 검증 (스테이징, CEO)
1. staging-0404.pickedby.ai/dashboard 접속
2. pickedby.ai 제품 수동 체크 1회 실행
3. 새로고침 → 4차원(Recognition/Category/CoRec/Web) 정상 표시 확인
4. (선택) Supabase 스테이징 SQL Editor: `SELECT product_name, unified_v15 FROM scores WHERE product_name='pickedby.ai' ORDER BY created_at DESC LIMIT 3`

### 영구 검증 (내일 04-21 오전)
1. 04-21 02:00 UTC cron 자동 실행 대기
2. 오전 대시보드 새로고침 → 여전히 4차원 표시 (롤백 없음) 확인

### 재발 방지 테스트 (Phase 1 TEST-01 필수 포함)
- TEST-CRON-UNIFIED-01: cron mock 실행 후 scores INSERT 에 unified_v15 필드 존재
- TEST-CRON-UNIFIED-02: UNIFIED_SCORE_V15=true 에서 cron 결과 unified_v15 not null
- TEST-CRON-UNIFIED-03: 연속 2회 cron 후 DB row 2건 모두 unified_v15 채워짐

### 프로덕션 플래그 활성화 체크리스트 (v1.5 정식 런칭 시)
- [x] 버그 수정 프로덕션 배포 (오늘 완료)
- [ ] TEST-CRON-UNIFIED-01~03 통과
- [ ] scores 테이블 unified_v15 컬럼 확인 (D1 마이그레이션 완료됨)
- [ ] 기존 null row 재계산 cron 1회 실행 (선택)
- [ ] CEO 승인
- [ ] wrangler.toml 에 `UNIFIED_SCORE_V15 = "true"` 추가 + 프로덕션 재배포

---

## 📝 결정 사항

- **특별 관리 지정**: 본 버그는 빅파이 1.5 정식 런칭 일정에 직접 영향 → `pickedbyAI/docs/bugs/` 폴더 신규 생성, 재발 시 즉시 리포트 갱신
- **프로덕션 플래그 정책 유지**: `UNIFIED_SCORE_V15` 프로덕션 활성화는 별도 CEO 승인 필요 (코드만 선제 배포하여 나중에 플래그만 켜면 동일 버그 재발 없음)
- **FE 폴백 경고 도입 보류**: `console.warn('[SCORE] unified_v15 missing')` 추가는 TEST-01 완료 후 검토

---

## 🎯 교훈 (영구 기록)

1. 이원 저장 경로는 반드시 동일 스키마로 (FE saveHistory ↔ 서버 cron) — 서로 다른 필드 저장하면 자정마다 롤백 발생
2. 플래그 분기는 코드의 모든 경로를 커버 (`/v1/check` + cron + auto-check + retry 4곳)
3. "롤백된다"는 CEO 표현 = 코드 롤백이 아니라 **데이터 롤백** (매일 덮어쓰기)
4. 빅파이 교훈 #6 "CEO 직관 신뢰" 재확인

---

## 🔁 다음 액션 (04-21 화)

1. CEO 스테이징 E2E 검증 (onboarding + 수정된 대시보드 4차원 표시)
2. T5 D3 DOMAIN-SEL-01 착수 (Shell 상단 도메인 드롭다운)
3. UI-SHELL-INTEGRATE-01 Phase B 병행
4. Phase 1 TEST-01 에 TEST-CRON-UNIFIED-01~03 통합 설계

---

*2026-04-20 세션 — CPO 긴급 대응*
