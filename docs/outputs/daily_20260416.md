# Daily Session Report — 2026-04-16
> pickedby.ai | CPO + Dev Agent | 데스크탑 Claude

---

## 세션 요약

총 커밋 41개 (04-15 19:59 ~ 04-16 00:39). 다수 세션에 걸쳐 대시보드 전면 개선 + 블로그 추가 + 모바일 버그 수정 + 점수 동기화 버그 수정.

**현재 상태:** 스테이징 배포 완료, QA 25/25 PASS. 프로덕션 배포 CEO 승인 대기.

---

## 1. 대시보드 대규모 개선 (dashboard.html — 19개 커밋)

### 1-1. BETA 배지 + CHANGELOG 카드 통합
- 로고 옆 BETA 배지 추가 (index.html + dashboard.html)
- CHANGELOG 카드와 Beta CTA를 하나로 통합 (중복 제거)
- 베타 미참여자만 CTA 표시

### 1-2. Settings 모달 — 베타 참여 관리
- BETA PROGRAM 섹션 추가
- `settingsBetaCancel()` / `settingsBetaJoin()` 함수
- Leave Beta / Join Beta 토글

### 1-3. Skip & Check UX
- URL 미입력 시 버튼 텍스트 "Check" → "Skip & Check" 자동 변경
- `updateCheckBtn()` + `oninput` 이벤트

### 1-4. CSP 수정
- `connect-src`에 `https://*.perceptdot.workers.dev` 추가
- 스테이징 API "Check failed" 오류 해결

### 1-5. CAV(Check AI Visibility) 간소화
- AI probe 섹션 제거 (My Products로 이동)
- OWNER DATA 섹션 제거 ("View →" 버튼 동작 불가 → 삭제)
- "Install SDK" 링크 제거 (SDK 미구현)
- 결과: 점수 + 차원 + 공유 버튼 + 피드백만 표시

### 1-6. My Products 확장 패널 강화
- **Direct AI Measurement** 섹션 추가 — Perplexity / ChatGPT / Gemini별 Recommended/Known/Unknown
- **OWNER DATA** 정리:
  - "Coming soon" → Score Momentum 실데이터 (pts vs last check)
  - co_recommendations / Query Coverage → "Collecting data..."
  - "Install SDK" 제거

### 1-7. 점수 동기화 수정 (핵심 버그)
**근본 원인:** API가 `score`(raw 22)와 `probe_score`(AI 32)를 따로 반환. 화면은 probe_score 표시, DB에는 raw score 저장.

| 시도 | 방향 | 결과 |
|------|------|------|
| 1차 | 저장 조건 변경 (URL/도메인 체크) | ❌ 미해결 |
| 2차 | loadHistory() 항상 호출 | ❌ 미해결 |
| 3차 | Optimistic update 추가 | ❌ 미해결 |
| 4차 | console.log 디버깅 | score:22 발견 → 근본원인 확인 |
| 5차 | `savedScore = useProbe ? probe_score : score` | ✅ 해결 |

**최종 수정:**
- `savedScore` 계산: `probe_count >= 3`이면 `probe_score` 사용
- `lastCheckResult.score = savedScore`
- Optimistic update에서도 `savedScore` 사용
- 로그인 시 항상 저장 (`if (currentUser)`)
- `autoSave` 점수 방향 무관 항상 업데이트

**교훈:** 추측으로 코드 변경 금지. console.log로 실제 데이터 값 먼저 확인. (feedback 메모리 저장 완료)

---

## 2. 모바일 네비 버그 수정 (22개 HTML 파일)

**증상:** 모바일에서 ABOUT 드롭다운 + 햄버거 메뉴 동시 표시 → UI 깨짐
**원인:** `.nav-links > a { display: none }` 규칙이 `<div>` 자식(ABOUT 드롭다운)을 커버하지 않음
**수정:** `.nav-links > a, .nav-links > div { display: none !important; }` — 22개 파일 일괄 적용

---

## 3. 블로그 — 점수 12→40 포스트

- **신규:** `blog/score-went-from-12-to-40/index.html` + `cover.png`
- 빌딩인퍼블릭 스타일: Day 0 (12점) vs Day 11 (40점) 비교
- 5차원 점수표 포함
- `sitemap.xml` 추가
- `blog/index.html` 카드 추가 (QA에서 누락 발견 → 수정)

---

## 4. 도메인 등록 기능 (ENGINE-06)

- `api/src/index.ts` — 도메인 등록 / 검증 API 엔드포인트
- `landing/register.html` — 도메인 등록 페이지 (신규)
- `docs/domain-verification-spec.md` — 인증 스펙
- `docs/migrations/domains_schema.sql` — Supabase 스키마

---

## 5. QA 결과

```
보안:       ✅ PASS (키 노출 0, CSP 정상, XSS 방어)
API:        ✅ PASS (/v1/check 정상, score+probe_score 반환)
페이지:     ✅ PASS (8개 전체 200 OK)
모바일:     ✅ PASS (22개 파일 수정 확인)
접근성:     ✅ PASS (viewport, alt, aria-label)
코드품질:   ✅ PASS (console.log 제거, 미사용 ID null 체크)
블로그목록: ✅ PASS (수정 후)
총: 25/25 PASS, 0 FAIL
```

---

## 변경 파일 목록

| 파일 | 변경 유형 |
|------|-----------|
| `landing/index.html` | BETA 배지, 모바일 네비 |
| `landing/dashboard.html` | CHANGELOG, Settings 베타, Skip&Check, CSP, CAV 간소화, AI Measurement, OWNER DATA, 점수 동기화 |
| `landing/register.html` | 신규 — 도메인 등록 페이지 |
| `landing/blog/score-went-from-12-to-40/index.html` | 신규 — 블로그 포스트 |
| `landing/blog/score-went-from-12-to-40/cover.png` | 신규 — 커버 이미지 |
| `landing/blog/index.html` | 새 글 카드 추가, 모바일 네비 |
| `landing/sitemap.xml` | 새 URL 추가 |
| `api/src/index.ts` | 도메인 등록/검증 API, beta API |
| `docs/domain-verification-spec.md` | 신규 — 스펙 문서 |
| `docs/migrations/domains_schema.sql` | 신규 — DB 스키마 |
| 블로그 16개 + manifesto + methodology + llms-generator | 모바일 네비 수정 |

---

## 배포 상태

| 환경 | 상태 | Version ID |
|------|------|------------|
| 스테이징 FE | ✅ 배포 완료 | 6f4ee789 |
| 스테이징 API | ✅ 배포 완료 | (기존) |
| 프로덕션 FE | ⏳ CEO 승인 대기 | — |
| 프로덕션 API | ⏳ CEO 승인 대기 | — |

---

## [CPO] 작업 완료 — 대시보드 전면 개선 + 모바일 버그 수정 + 블로그 추가 + 점수 동기화 해결. QA 25/25 PASS. 프로덕션 배포 대기. (데스크탑 Claude, 00:45 KST)

---

## 세션 2 (10:00~11:00 KST)

### 완료
1. **타임존 설정 기능** — Settings > ACCOUNT > Timezone 드롭다운. 23개 주요 타임존. localStorage(`pba_timezone`) 저장. 브라우저 자동 감지 기본값. 차트 X축·테이블 날짜·날짜비교 전체 적용
2. **스테이징 cron trigger 추가** — `api/wrangler.toml` `[env.staging.triggers]` 추가
3. **프로덕션 배포 차단 훅** — `settings.local.json` deny에 `git push origin main`, `wrangler deploy` 추가 → 물리적 차단
4. **배포 규칙 메모리 업데이트** — 스테이징 전권 위임, 프로덕션만 CEO 승인 (2026-04-16 CEO 재확인)

### 발견 이슈
- cron: 프로덕션·스테이징 모두 CF에 등록되어 있었으나, 4/14는 등록 시점(02:07 UTC) > 실행 시점(02:00 UTC)이라 누락
- 프로덕션 main에 4/15 CEO 미승인 머지 이력 (이전 세션 위반)

### 배포
| 환경 | 상태 |
|------|------|
| 스테이징 FE | ✅ 893da941 |
| 스테이징 API | ✅ 4b741775 (cron schedule 확인) |
| 프로덕션 | ⏳ CEO 승인 대기 |

### 대기
- 4/16 UTC 02:00 cron 실행 결과 확인 중

## [Dev] 세션 2 완료 — 타임존 설정 + 배포 차단 훅 + cron 수정. (데스크탑 Claude, 10:55 KST)
