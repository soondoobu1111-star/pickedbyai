# Daily 2026-04-23 — pickedby.ai

> 이전 세션: `daily_20260422.md` (빅파이 1.5.1 Volume Metrics + Trend Daily 30d 통일)
> 현재 집중: 빅파이 1.5 Phase 1 (DEPLOY-GATE-01 전 프로덕션 금지)

---

## [CPO] 세션 1 — Trend/Volume 차트 정합성 + Category 자동 추론 강화 (데스크탑 Claude, 00:21 KST)

### CEO 지시 (스크린샷 첨부)
Trend 차트 관찰 후 4건 보고:
1. 차트 짤림 (X축 라벨 빽빽, 연도까지 표시되어 공간 부족)
2. Category 파란선 바닥 고정(=0) · "이럴꺼면 뭐하러 제공해!"
3. 툴팁이 포인트별로 분리 · 같은 날 Total/Recognition/Category 합쳐서 표시 요청
4. Volume Trend — 데이터 전혀 안 보임

"대책세워. 해결책 마련해!" → 무료 대안만 진행 지시.

### 진단

| 현상 | 실제 원인 |
|---|---|
| X축 연도까지 표시 | `toKSTDate()` = `YYYY-MM-DD` 풀 문자열 그대로 라벨 |
| Category 0 바닥 | `extractCategoryHint` 패턴 1개뿐. pickedby.ai 설명이 해당 패턴 안 맞음 → `rankings = []` → computeCategoryRanking = 0 저장. 설계상 버그 아니나 **UX 혼동 유발** |
| 툴팁 분리 | Chart.js 기본 모드 `nearest` + `intersect:true` |
| Volume 전무 | `probe_logs` 누적 부족. empty state 안내 빈약("Come back in 24h" 한 줄뿐) |

### 무료 대안 대책 5건 구현

| # | 대책 | 위치 | 비용 |
|---|---|---|---|
| A | X축 `MM-DD`, 1/1만 연도 · `maxRotation:0 + autoSkip` | `landing/dashboard.html` (Trend+Volume 공통 헬퍼 `formatChartDateTick`) | 0 |
| B | 툴팁 `mode:'index', intersect:false` + 인터랙션 모드 통일 | `drawTrendChartV2` / `drawVolumeChart` | 0 |
| C | `extractCategoryHint` 3패턴 확장(원본 + 대시/em-dash + 콤마 동격) + 실패 시 **Gemini Relay fallback**(Service Binding, 무료) | `api/src/unifiedScoreAdapter.ts` | **0** (Gemini Relay 무료 티어) |
| C-2 | `buildTrendSeries`에서 `breakdown.status === 'not_measured'/'insufficient_data'` row 집계 제외 → `category: null` 반환 → 차트에서 자동 숨김 | `api/src/index.ts:buildTrendSeries` | 0 |
| D | `trend-category-notice` DOM 박스 신설 + `drawTrendChartV2`에서 Category 데이터 전무 시 overlay 데이터셋 제외 + 안내 표시 | `landing/dashboard.html` | 0 |
| E | Volume empty 메시지 재설계 ("BUILDING UP VOLUME" + 3칩 메타 + manual Check 힌트) | `landing/dashboard.html:volume-empty` | 0 |

### 기술 설계 메모 — Category Hint Rescue (C)
1. **1단계 Pattern 확장**: 기존 `"{name} is a X tool"`만 있던 패턴에 `"{name} — X for Y"` (대시/em-dash/콜론), `"{name}, an X,"` (콤마 동격) 2종 추가. 모두 동일 stop-word 후처리 공유.
2. **2단계 Gemini Relay Rescue**: 1단계 실패 시 `env.GEMINI_RELAY.fetch(...)` Service Binding 호출. Prompt = 2-4 단어 카테고리 + "unknown" 허용 + 정규식 검증. 타임아웃 8s.
3. **가격 근거**: Relay Worker는 이미 무료 Gemini Flash 라우팅. 추가 비용 0. Worker CPU ms만 소폭 증가(무시 가능 범위).

### 검증

- `npx tsc --noEmit`: 0 errors
- `npx vitest run`: **14 tests PASS** (invariants 2 / math 3 / pass-indicator 6 / savedScore 1 / jsonb 2)
- `./scripts/deploy-staging.sh`: API + FE + Gemini Relay 3종 배포 성공
  - API Version `c4e4271e-da19-416c-9df7-398089610355`
  - FE Version `874a6d8a-1758-4be6-a1d8-ee4414cc7d79`
- `curl -sL https://staging-0404.pickedby.ai/dashboard | grep …` → 신규 4토큰 모두 반영 확인:
  `formatChartDateTick` / `trend-category-notice` / `BUILDING UP VOLUME` / `mode: 'index'`

### CEO 승인 필요 사항 (스테이징 시각 검증)

다음 시나리오 직접 확인 부탁드립니다:
1. Trend pane — X축 라벨 `MM-DD` 형식, 포인트 hover 시 Total+Recognition+Category가 한 툴팁에 합쳐 표시
2. Trend pane — Category overlay 체크 시 파란 바닥선 대신 "CATEGORY AWAITING" 안내 박스 등장
3. Volume Trend pane — "BUILDING UP VOLUME" 메시지 + 3칩 메타 + manual Check 안내 확인
4. (선택) 새 Check 실행 시 Gemini Relay 경로로 카테고리 추론되는지 워커 로그 확인

### CEO 못박음 준수
프로덕션 배포 일체 금지(DEPLOY-GATE-01 전). 이번 변경도 **스테이징만**.

### 영구 교훈 추가 (빅파이 1.5 §15 후보)
12. **0점은 "안 측정"과 다르다** — UI에서 동일하게 보이지만 근본 상태가 다름. `breakdown.status` 같은 메타를 함부로 버리지 말 것. 측정 실패는 측정 실패로 표현.
13. **무료 인프라 두 번 쓰기** — Gemini Relay는 Probe용으로 이미 운영 중. Category Hint Rescue에 재활용하니 추가 비용 0. "무료 자산 먼저 조사" (§15 #4) 재확장.

---

## 남은 액션 (CEO 검증 후)

- [ ] CEO 스테이징 시각 검증 → 이상 없으면 프로덕션 배포 게이트 준비
- [ ] Ground Truth 10개 실측 (빅파이 1.5 §5 GT-01)
- [ ] DEPLOY-GATE-01 13개 기준 체크

---

## [CPO] 세션 2 — TAVILY-CRON-LITE-01 (크레딧 보호) (데스크탑 Claude, 09:52 KST)

### 트리거 — Tavily 80% 경고 메일
- **Researcher Plan 835/1,000 크레딧** (83.5% 소진, 9일 남음)
- 스크린샷 검증: **PAYGO 토글 OFF 확인됨** → 되돌릴 수 없는 과금 리스크 해소
- CEO 지시: "b 진행" (옵션 B 승인)

### 원인 분석
```
dailyRefresh (매일 00:01 KST)
  └── runEngine(제품) × N개
       └── searchTavily × 3 쿼리 ("name" + review + vs)
                + 미발견 시 targeted 1쿼리
                = 제품당 평균 3.2 쿼리
```
retry queue(`runInternalCheck`)도 full 3쿼리로 호출되던 중.

### 수정 내역 — 1-line spec: `runEngine` cron 경로 Tavily 1쿼리로 축소
- `api/src/index.ts:548` — `runEngine(env, name, url, opts?: { tavilyLite?: boolean })` 시그니처 확장 (BC 유지)
- `api/src/index.ts:550-559` — lite 모드: `"${name}"` 1쿼리만 유지, review·vs 쿼리 제외
- `api/src/index.ts:984` — `dailyRefresh` 호출 `{ tavilyLite: true }` 전달
- `api/src/index.ts:2449` — `runInternalCheck` (retry queue·auto-check) 호출 `{ tavilyLite: true }` 전달
- `/v1/check` (line 903) — **기본값 유지** = manual 유저 체크는 3쿼리 full 유지

### 예상 효과
| 경로 | 기존 쿼리/제품 | 변경 후 | 감축율 |
|---|---|---|---|
| manual `/v1/check` | 3.2 | 3.2 | 0% (UX 우선) |
| cron `dailyRefresh` | 3.2 | 1.2 | **62.5% ↓** |
| retry queue `runInternalCheck` | 3.2 | 1.2 | **62.5% ↓** |

남은 165 크레딧 (04-30 리셋까지 9일) → lite 반영 후 **여유 3배 확장**.

### 안전장치 유지
- `targeted retry` (미발견 시 1쿼리 추가) — lite에서도 유지. Web 차원 정확도 90% 수준 보존
- `.catch(() => [])` — Tavily 호출 실패 시 조용히 빈 배열 반환 (기존)
- Web 차원(10점) = 크레딧 소진 시 0점 자동 fallback

### 검증
- `npx tsc --noEmit`: 에러 0
- `npx vitest run`: **14/14 PASS** (Invariants 2 / Math 3 / Pass Indicator 6 / savedScore 1 / JSONB 2)
- `./scripts/deploy-staging.sh`: API 배포 완료
  - API Version `4fc2f190-4ff7-420d-8f90-8e12e71875ea`
  - FE Version `90255a45-ecb4-4bc7-b385-51e439f307ae` (변경 없음)
- 실제 효과 검증: 내일 04-24 00:01 KST cron 실행 후 워커 로그 `[ENGINE-05] 1 queries` 확인 예정

### CEO 못박음 준수
프로덕션 배포 일체 금지(DEPLOY-GATE-01 전). 이번 변경도 **스테이징만**. 다만 프로덕션 크레딧도 동일 위협이므로 **가능하면 프로덕션 배포 우선순위 상향 고려 필요** (별도 CEO 판단).

### 영구 교훈 후보 (§15)
14. **외부 API 사용량 알림은 되돌릴 수 없는 과금 전조** — PAYGO 토글 확인 먼저. 크레딧 대시보드 정기 모니터링을 CRON 기반 체크로 자동화 필요(다음 세션 후보: Tavily usage self-probe endpoint).

---

## [CPO] 세션 3 — 전체 현황 싱크 (데스크탑 Claude, KST)

### 스캔 범위
- `pickedbyAI/docs/bigpie-v1.5.md` 전체
- `pickedbyAI/docs/outputs/daily_20260423.md` (오늘치)
- `memory/backlog.md` 전체
- `~/.claude/projects/.../memory/MEMORY.md`
- `pickedbyAI/docs/outputs/` 디렉토리 전체 파일 목록

### 발견된 누락 기록 및 처리

| 항목 | 파일 | 처리 |
|---|---|---|
| CHART-01 — D6 TREND-01로 구현 완료 | backlog.md | [ ] → ✅ |
| UI-OVERVIEW-12FULL 설계 문서 | backlog.md | [ ] → ✅ |
| D7 스펙 문서 | backlog.md | [ ] → ✅ |
| GT-01 리스트 (실측 보류) | backlog.md | [ ] → 📋 |
| MODOO-01 지원서 초안 | backlog.md | [ ] → ✅ |
| MODOO-P0 흔적 정리 | backlog.md | [ ] → ✅ |
| TEST-01 score.test.ts | backlog.md | [ ] → ✅ (TEST-CRON-UNIFIED-01~03으로 완료) |
| MEMORY.md 날짜 | MEMORY.md | 04-18 → 04-23 |
| MEMORY.md NEXT ACTION 정리 | MEMORY.md | 완료된 1~27번 항목 archive, 미완료만 유지 |
| backlog.md 업데이트 날짜 | backlog.md | 2026-04-18 → 2026-04-23 |

### 현재 스테이징 버전 확인
- API: `4fc2f190` / FE: `874a6d8a`
- tsc: 0 errors / vitest: 14/14 PASS

### D6~D11 잔여 스프린트 (변경 없음)
- D7 PROBE-REDESIGN-01 (04-24) — 스펙 완성
- D8 Rivals+GT 실측 (04-25) — 리스트 완성
- D9 랜딩 리뉴얼 (04-26~27)
- D10 모바일 (04-28)
- D11 DEPLOY-GATE-01 + CEO 검증 (04-29) 🔴

---

## [CPO] 세션 4 — Tavily 키 확인 + 프로덕션 선반영 (데스크탑 Claude, KST)

### 트리거
이전 세션(TAVILY-CRON-LITE-01) 종료 시 미확인 사항:
"Tavily API 키가 스테이징·프로덕션 공유인가 / 별도인가?" → **CEO 직접 확인: 1개 (공유)**

### 확인 결과 및 판단
| 항목 | 내용 |
|---|---|
| Tavily 키 | **1개 공유** (스테이징 + 프로덕션 동일 버킷) |
| 현재 소진 | 835/1,000 크레딧 (83.5%) |
| 프로덕션 상태 | TAVILY-CRON-LITE-01 미반영 → cron 3쿼리 그대로 소진 중 |
| 04/30 리셋까지 | 9일 / 잔여 165크레딧 → 소진 위험 |

### CEO 승인 — DEPLOY-GATE-01 예외 적용
"예외로 선반영 실시한다. 실시" → **TAVILY-CRON-LITE-01 단건 프로덕션 선반영 승인**

### 배포 내역
```
cd pickedbyAI/api
npx wrangler deploy  (프로덕션, env 기본값)
```
- **Worker**: `pickedbyai-api` (프로덕션)
- **Version ID**: `1e52e42b-61c0-4573-9357-e00fa6b6794b`
- **URL**: `https://pickedbyai-api.perceptdot.workers.dev`
- **cron**: `0 15 * * *` (KST 00:01, BUG-CRON-CONFLICT-01 1분 오프셋 유지)

### 효과
- 다음 cron(04-24 KST 00:01)부터 자동 경로 Tavily 3→1쿼리로 즉각 적용
- 잔여 165크레딧 04/30 리셋까지 안전 구간 확보 (여유 ~3배)
- 수동 `/v1/check` 3쿼리는 그대로 (UX 우선)

### CEO 못박음 준수 기록
DEPLOY-GATE-01 통과 전 프로덕션 배포 금지 원칙 하에, **크레딧 소진 = 과금 위험** 근거로 단건 예외 승인.
예외 범위: API `index.ts` 2줄(`{ tavilyLite: true }`)만. FE 배포 없음.

### 버전 최종 현황
| 환경 | API | FE |
|---|---|---|
| 스테이징 | `4fc2f190` | `874a6d8a` |
| **프로덕션** | **`1e52e42b`** (TAVILY-CRON-LITE-01) | 변경 없음 |
