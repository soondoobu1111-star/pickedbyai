# daily_20260502 — pickedby.ai Day 27

## 작업 환경
데스크탑 Claude (Opus 4.7) — CPO/Dev

---

## Session 1 — BUG-SCORE-CONSISTENCY-01 근본 fix + 06차 프로덕션 배포 (21:48 ~ 22:35 KST)

### CEO 트리거
> "처음엔 pickedby.ai score 5점이었는데 리프레시하니까 55점으로 변함. 진짜 점수 좆같이 작동하네. 일관성이 없어. 자동이 제대로 작동이 안되잖아. 이부분 몇번째 수정하냐?"

= 4번째 score 일관성 회귀. 매번 표면 fix만 하고 근본 안 잡은 결과.

### Skill Discipline 적용
- `/investigate` Skill tool 실호출 (Iron Law: 근본 원인 없으면 fix 금지)
- 4단계 (Investigate → Analyze → Hypothesize → Implement) 준수

### Phase 1-3: 가설 검증 (DB 직접 조회)

production scores 테이블 쿼리 결과:
```
created_at        | score | 분석
2026-05-02 12:51  |   55  | live /v1/check (refresh, 정상)
2026-05-01 15:01  |    5  | ⚠️ CRON ANOMALY (-40점, baseline 11%)
2026-04-30 03:12  |   45  | cron 정상
2026-04-29        |   45  | 정상
04-28 ~ 04-23     |   45-55  | 안정적 baseline
```

**가설 100% 확정**: 05-01 15:01 cron이 부분 실패한 score=5 row 저장 → FE는 desc 정렬 후 score>0 첫 row 픽 → 5점 잘못 표시. Refresh → live /v1/check → 55점 정상.

### 근본 원인 (2개)

#### ROOT-1: FE `isValidScoreRow` threshold 너무 관대
- 기존: `s > 0` 만 valid → cron 부분 실패 score=5도 통과
- 04-29 L2-13에서 score=0/null만 거부, 5는 못 거름 (부분 fix)

#### ROOT-2: cron이 부분 실패 row를 정상처럼 저장
- TAVILY-CRON-LITE-01 (04-23): Tavily 1쿼리만 → 실패 시 Web Authority=0
- Gemini/Perplexity 부분 실패 시 Direct Recognition도 0~부분 점수
- 모든 차원이 부분 실패하면 score 5~10 같은 비정상 row 생성
- cron은 flag 없이 그대로 저장 → FE가 정상으로 픽

### Fix 1: FE `isValidScoreRow` baseline-aware 강화

`landing/dashboard.html:2579`
```javascript
function isValidScoreRow(row, baselineScore) {
  if (!row) return false
  if (row.flagged === true) return false        // BE flagged 우선
  const s = _scoreOfRow(row)
  if (!Number.isFinite(s) || s <= 0) return false
  if (s < 10) return false                       // 절대 floor
  if (Number.isFinite(baselineScore) && baselineScore >= 20 && s < baselineScore * 0.3) return false
  return true
}

function computeBaseline(rows) {
  // 직전 valid (s>=10) 점수 중앙값
  const validScores = rows.map(_scoreOfRow).filter(s => Number.isFinite(s) && s >= 10)
  if (validScores.length === 0) return 0
  validScores.sort((a, b) => a - b)
  const mid = Math.floor(validScores.length / 2)
  return validScores.length % 2 === 0 ? (validScores[mid - 1] + validScores[mid]) / 2 : validScores[mid]
}

function getValidLatestFromDesc(rowsDesc) {
  const baseline = computeBaseline(rowsDesc.slice(1))  // 첫 row 제외 (anomaly 가능성)
  for (const r of rowsDesc) {
    if (isValidScoreRow(r, baseline)) return r
  }
  return rowsDesc[0] || null
}
```

### Fix 2: BE cron anomaly 감지 + flagged 마킹

`api/src/index.ts`:
- `getRecentBaselineScore(env, productName, userId, daysBack)` 헬퍼 추가 (직전 7일 valid 중앙값)
- cron INSERT 직전 baseline 비교: `savedScore < baseline * 0.3` → `flagged: true`
- KPI counter `kpiAnomaly` 추가 → `[CRON KPI]` 로그에 `anomaly=N` 표시

### Migration: `scores.flagged` 컬럼

`docs/migrations/scores_flagged_column_20260502.sql`:
```sql
ALTER TABLE scores ADD COLUMN IF NOT EXISTS flagged BOOLEAN NOT NULL DEFAULT FALSE;
CREATE INDEX IF NOT EXISTS scores_flagged_idx ON scores(flagged) WHERE flagged = TRUE;

-- 백필: 05-01 pickedby.ai score=5 row → flagged=TRUE
UPDATE scores SET flagged = TRUE WHERE id IN (
  SELECT s.id FROM scores s
  WHERE s.product_name = 'pickedby.ai' AND s.score < 10
    AND DATE(s.created_at) BETWEEN '2026-05-01' AND '2026-05-02'
);
```

CEO 검증 (프로덕션 SQL Editor 결과):
```
05-02 12:51 | 55 | flagged=false  ✅
05-01 15:01 |  5 | flagged=TRUE   ✅ (백필 PERFECT)
04-30 03:12 | 45 | flagged=false  ✅
04-29       | 45 | flagged=false  ✅
04-28       | 45 | flagged=false  ✅
```

### Fix 3: vitest 회귀 테스트 21 cases

`api/src/scoreConsistency.test.ts` (신규):
- BUG-SCORE-CONSISTENCY-01 reproduction (05-02 데이터 그대로)
- flagged 우선 (3 cases)
- 절대 floor s>=10 (3 cases)
- baseline 가드 (4 cases)
- computeBaseline 정확성 (6 cases)
- 05-02 시나리오 자체 (5 cases)

검증: **48/48 PASS** (기존 14 + prescription 13 + scoreConsistency 21) / tsc 0 에러

### Fix 4: 영구 정착 룰

`memory/feedback_score_consistency.md` 신설:
- 3중 방어 (BE baseline / scores.flagged / FE isValidScoreRow)
- 회귀 테스트 의무 (21 vitest)
- When to apply / When NOT
- 사례 기록 (05-02 BUG-SCORE-CONSISTENCY-01)

`MEMORY.md` 피드백 규칙 인덱스 추가.

### 가짜 알람 정정 — BUG-UNIFIED-FINAL-NULL

DB 검증 시 `unified_v15->>'final'` NULL 발견 → 별도 버그로 의심.

조사 결과: **`final` 필드는 코드 어디에도 없음**. 빅파이 1.5 §2 "sum == final" 표현은 docs/comments 의미상 표현이고, 실제 필드는 `unified_v15.score`. 모든 row에서 `score` 필드 정상 채워짐 (sum dimensions == score 수학적 일관성 PASS).

→ **BUG-UNIFIED-FINAL-NULL은 가짜 알람**. backlog 등록 불필요.

### 배포 (06차)

**스테이징**: Version `995d054a`
- Migration 별도 적용 (CEO 직접): xzecybljfipmmzzzfnit
- API + FE 배포

**프로덕션**: API `1df48af6` / FE `9363e3d0`
- deploy-prod.sh 5/5 게이트 PASS
- Migration 별도 적용 (CEO 직접): pfrcppgecqsbnhkkjkbd + 백필 검증 PASS
- 라이브 검증:
  - FE `flagged === true` 체크: 1건 ✅
  - FE `computeBaseline` 함수: 3건 ✅
  - API health: 200 OK ✅

### 즉시 효과

배포 직후 사용자가 dashboard 열면:
- 05-01 score=5 row가 flagged=TRUE → FE 자동 제외
- 05-02 score=55 row가 representative로 픽 → 정상 표시
- CEO refresh 안 해도 자동으로 55점 표시

다음 cron (05-03 00:01 KST):
- baseline 가드 자동 동작
- anomaly 감지 시 `[CRON KPI] ... anomaly=N` 로그 + flagged=true 저장

### 빅파이 레이어
- `phase_1` Production Hardening — score 일관성 회귀 게이트 영구 정착
- `moat_data` 보호 — 처방 효과 데이터의 baseline 진단 정확도 ↑

### 누적 회귀 패턴 (4번째 → 게이트 닫힘)

| # | 회귀 | 일자 | 표면 fix → 진짜 원인 |
|---|------|------|---------------------|
| 1 | BUG-AX-WRONG-PRODUCT | 04-28 | refresh handler 변경 → 캐시 vs live 분리 |
| 2 | L2-13 | 04-29 | score=0/null 거부 → threshold 부분만 |
| 3 | BUG-TIER-LABEL | 04-30 | label 매핑 fix → score 자체 일관성은 그대로 |
| **4** | **BUG-SCORE-CONSISTENCY-01** | **05-02** | **3중 방어로 근본 원인 2개 모두 닫음** |

### 다음 액션 (05-02 ~ 05-03)
- ⏳ Reddit 카르마 24h 변화 측정 (현재 106 → ?)
- ⏳ LinkedIn Day 24 댓글 답변
- ⏳ 05-03 00:01 KST cron 로그 확인 (anomaly=0 또는 적절 감지 검증)
- ⏳ AEO Doctor 봇 라이브 (BOT-MKT-01) — 05-03 일요일 풀가동
- ⏳ Phase 2b D5 TRACKING-01 (05-03 일요일)

---

*[CPO/Dev + /investigate skill] BUG-SCORE-CONSISTENCY-01 근본 fix (3중 방어) + 06차 프로덕션 배포 + 영구 룰 정착 + 21 vitest cases. 47분 소요. API `1df48af6` / FE `9363e3d0` — 2026-05-02 22:35 KST (데스크탑 Claude, Opus 4.7)*
